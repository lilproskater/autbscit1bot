* Rename command googleweather to google_weather in BotFather
* Rename command namaztoday to namaz_today in BotFather
* Add DB table for subjects (+ admin command /update_subjects that will work just like BotFather's /setcommands)
* Add admins table (+ admin command /update_admins that will work just like BotFather's /setcommands)
  - Handle cases when admin can remove himself!
* Lang files to make code cleaner?
